# Build firmware on push/PR; upload binaries as a short-lived artifact.
name: Build

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  firmware:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache PlatformIO
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
            .pio/libdeps
          key: ${{ runner.os }}-pio-${{ hashFiles('platformio.ini') }}
          restore-keys: |
            ${{ runner.os }}-pio-

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install PlatformIO
        run: pip install --upgrade platformio

      - name: Build
        run: pio run -e waveshare-esp32-s3-touch-lcd-2-1

      - name: Merge web-flash image
        run: pio run -t merge -e waveshare-esp32-s3-touch-lcd-2-1

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: plane-radar-waveshare-esp32-s3-touch-lcd-2-1
          if-no-files-found: error
          path: |
            .pio/build/waveshare-esp32-s3-touch-lcd-2-1/firmware-merged.bin
            .pio/build/waveshare-esp32-s3-touch-lcd-2-1/firmware.bin
            .pio/build/waveshare-esp32-s3-touch-lcd-2-1/bootloader.bin
            .pio/build/waveshare-esp32-s3-touch-lcd-2-1/partitions.bin
